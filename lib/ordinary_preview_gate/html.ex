defmodule OrdinaryPreviewGate.HTML do
  @moduledoc """
  Placeholder module for templates/components.

  For now, the preview gate is shipped as controller+plug; the host app owns the
  HEEx templates and layout. Once stabilized, we can move templates into this
  package and provide `use OrdinaryPreviewGate.HTML` to embed them.
  """
end
